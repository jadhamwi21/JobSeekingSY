import styled from "styled-components";

export const NoResultContainer = styled.div`
	display: grid;
	place-items: center;
`;

export const NoResultContentWrapper = styled.div`
	display: flex;
	flex-direction: column;
	align-items: center;
	justify-content: center;
	padding: 2em;
	color: var(--blue);
	font-size: 20px;
`;

export const EmojiElement = styled.p``;
